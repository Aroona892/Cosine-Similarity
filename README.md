# Cosine-Similarity
Computes pairwise cosine similarity between text documents using TF-IDF. Reads files from a folder, tokenizes them, converts to TF-IDF vectors, and calculates similarity scores for text analysis or document clustering.
